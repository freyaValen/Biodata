import { data } from "../constants";

const Profile = () => {
    return (
        <div className="profile">
            <h1>Profile</h1>
            <p>{data.desc}</p>
        </div>
    )
}

export default Profile